# 路径控制研究记录

## 当前结论

当前路线控制是开环系统：控制台根据占空比和标定满速估算左右轮速度，再用差速模型积分位置和航向；车上没有编码器，路线线程也没有视觉、IMU 或横向位置反馈。因此，实车偏离规划路线时，控制台看见的蓝色轨迹仍然只是估计轨迹，不能证明车身真实走过这条线。

主要误差按影响优先级排列：

1. 左右轮实际速度不一致，导致直行时航向持续漂移。
2. 电池电压、载荷、地面摩擦和轮胎打滑改变同一占空比对应的速度。
3. 原地转和弧线转的有效轮速不同，固定轮距不能同时准确描述两种动作。
4. 网络发送、PWM 响应、静摩擦和刹车造成指令生效与停止的时延。
5. 路线启动默认车头朝 +x；初始朝向偏差会被带入整条路线。
6. 路点控制使用默认 80 mm 容差，而前端相邻绘制点最小间距为 50 mm，拐角处会提前切弯。

## 已落地的基础能力

src/odometry.py 的开环模型现在支持：

- 左右轮独立速度增益 left_scale、right_scale；
- 左右轮死区 left_deadzone、right_deadzone；
- 状态接口返回这些参数，便于记录实验配置。

控制台路线接口和网页参数还支持：

- 路线启动时指定初始朝向 heading，单位为度；
- /api/odom_cfg 和 /api/route 传入左右轮增益与共享死区；
- 浏览器 localStorage 保存这些参数，刷新页面不会丢失。

默认参数仍为左右增益 1、死区 0、初始朝向 0 度，所以没有标定数据时行为与原模型一致。

## 架空阶段的工作边界

架空状态只能验证软件逻辑、帧顺序、参数传递、路线状态机和数学模型，不能测量真实速度、转弯半径、打滑或停止距离。此阶段不应凭空修改满速、轮距、35 度原地转阈值、58% 内侧最低速或 80 mm 容差。

架空阶段应固定一组参数，使用假机器人和离线积分复现：直行、原地转、弧线转、S 形路线、急停和路线超时。每个场景同时记录规划路点、估计轨迹、轮速命令和最终估计误差。

## 落地后的最小标定流程

1. **死区**：左右轮分别从低占空比开始递增，记录刚开始持续转动的占空比；取略高于该值的死区参数。左右轮不要强行共用同一个死区。
2. **速度**：在同一电池电压、载荷和地面上，用 60% 和 100% 占空比分别直行固定时间，测量左右轮实际距离。先用平均速度更新 vmax，再用左右轮相对平均速度更新 left_scale 和 right_scale。
3. **轮距**：用实际原地转角反算有效轮距。若原地转预测角度大于实测角度，应增大模型轮距；反之减小。原地转和弧线转应分别记录，判断是否存在动作相关的等效轮距。
4. **停止**：测量从 STOP 到车体停止的距离。若停止距离不可忽略，应在路线终点前预留停止距离，不能靠增大路点容差掩盖。
5. **重复性**：每个动作至少重复 5 次，记录均值和离散程度。参数只对固定电压、载荷和地面条件负责，换条件后重新验证。

## 后续控制策略

在没有任何真实位姿反馈前，最值得先做的是校准和路线几何处理：减少过密路点、用线段或平滑曲线跟随、在终点减速并处理停止距离。继续堆叠积分补偿不能消除累计误差。

如果需要真正压低长期偏差，优先级是：

1. 左右轮编码器：提供轮速和里程闭环；
2. IMU：提供短期航向反馈，抑制转向漂移；
3. 相机、标记点或视觉里程计：提供横向误差和真实轨迹反馈；
4. 编码器距离触发拍照：把拍照从时间触发改为位移触发。

当前 liuviking 控制台只有视频流，没有真实拍照触发接口；AgriScout 的采样点和照片协议属于平台侧数据结构，不能替代车上采集链路。

## 本轮控制器修正

本轮路线执行不再把每个路点当作独立目标点追逐，而是沿当前规划线段计算航向误差和横向误差：

- 路线决策周期从 150 ms 降到 50 ms；
- 原地转由 100% 降为 45% 占空比，避免一个周期跳过几十度；
- 航向误差较大时低速原地转，误差较小时用差速修正；
- 路点容差默认从 80 mm 降到 12 mm，服务端上限 40 mm；
- 前端会把旧版保存的 80 mm 默认值迁移到 12 mm；
- 原有底层控制延迟修复未回退，路线层只调整决策与估计模型。

这能修正架空模型里的切角和转向过冲，但仍不能证明真实车体轨迹准确；真实车体仍需要地面标定和位姿反馈。

## 拍照暂停把自动行走搞乱的第一性原理（2026-09-25 修复）

自动行走的唯一位置来源是开环积分（指令占空比 × 标定速度 × 时间）。拍照功能的停车
动作引入了破坏点：每到一个拍照点，路线线程握着控制台锁停车约 2s（云台/快照）；
里程计线程被锁憋住，锁一放开就用**陈旧 dt（约 2s）× 恢复行驶的指令**补积——每拍
一张白涨约 500mm 假位移，而拍照点间距只有 300mm → 拍照点被连续"喂"着走、路点提前
"到达"，自动行走全乱（实测 1800mm 路线 13s 拍完 7 组、估算冲到 2152mm）。

修复三处：① 里程计积分步长封顶 0.25s；② 拍照暂停开始即重锚里程计时钟（停车时间
不积成位移）；③ 连续行驶中的转向修正重发（实测 10–20Hz）不再重置 1s 反馈窗口。

真机复验（1600mm + 7 个拍照点，开放环）：拍照点全部落在估算 300/600/…/1600mm 上，
暂停期间估算冻结，终点 1594mm，note `完成`。

**节点先对齐再拍（2026-09-25，用户要求）**：拍照点要求车头与所在段方向夹角
≤ `PHOTO_ALIGN_DEG`(5°) 才停车拍；节点处本段终点照合并到下一段的 0mm 点，车先
转向下一段方向、对齐后才拍——即"下一个起点"的照片朝下一段路线的方向，不再到点
就拍旧朝向。

## 反馈门控与开放环开关（2026-09-25）

路线执行两种模式（`/api/route?...&fb=0/1`；页面"无反馈时继续（开放环）"勾选，
**默认勾选**）：

- **严格门控（fb=1）**：车端脉冲计数是"真的在动"的唯一凭据（桥只读
  `/api/motion_feedback`）；发车后约 1s 计数无变化（或接口不可用）→ STOP，note 记
  `无实际运动反馈（脉冲计数未变化）`；未观测到运动时后续拍照点不推进。
- **开放环（fb=0）**：不因计数停车、拍照点按开环估算推进——本车无编码器时的可用
  模式；蓝线/照片均为估算，不代表车体真实位置。

**本车硬件结论（2026-09-25，证据链）**：计数器是真实 I2C 读（板子地址 0x17、寄存器
0x13/0x14，反汇编 smbus 库字节码确认）；`i2cdetect` 板子在应答；但 WASD 转轮子
（轮子正常、电池带电机）期间计数全程 0/0（10 分钟观察窗），电压寄存器 0x12 = 0，
角度回读恒 0xAF。结论：这块 PWR.A53（此版本）没有可用的模拟回读通路，严格门控在
本车永不通过——页面默认开放环即因此。沿用文档旧结论：诊断以目视为准。

## 半自动模式（"show" 勾选框，2026-09-25）

勾选后路线不再自动转弯：直线段照常自动走（中途拍照点照常自动拍），到节点
（路点）先按进站朝向拍完节点照，然后**交出控制权**——页面提示"等待人工转向"，
用 WASD 手动把车转到与下一跳路段平行，点「已对准，继续」后系统**把位置归零到
该节点坐标、朝向直接设为下一段方向**（转身过程不估计），再自动走下一段直线。
等待期间手动指令放行、松键的 STOP 只停车不中止路线（停止路线按钮仍然有效）。

真机验证（300mm→节点→对准 90°→300mm）：等待标记/手动放行/对准归位(theta 90.0°、
位置归零 (300,0))/恢复完成 全部符合；单测 `test_semi_auto_...` 覆盖节点拍照+等待+
对准恢复全流程；pytest 180 全过。

## 验收顺序

先用假机器人验证参数和状态机，再做架空电机方向与死区测试，最后在地面执行标定路线。每次实车测试都要保存电压、载荷、地面、参数、规划路点、估计轨迹、实测终点和照片序号；没有这些条件记录，路线偏差无法归因。
